import { Layout } from "../Layout/Layout";
import { Calendar } from "../calender/Calendar";
const CalendarDate = () => {
  return (
    <Layout>
      <Calendar />
    </Layout>
  );
};
export default CalendarDate;
